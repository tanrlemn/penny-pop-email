{
  "name": "sequence-savings-sentinel",
  "version": "0.1.0",
  "private": true,
  "description": "No-UI savings trend sentinel for Sequence balances (GREEN/YELLOW/RED email).",
  "type": "commonjs",
  "main": "dist/index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "npm run build && node -r dotenv/config dist/index.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "nodemailer": "^6.9.16"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "@types/nodemailer": "^6.4.17",
    "typescript": "^5.6.3"
  }
}


